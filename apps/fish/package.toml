requires = ["family=unix"]

[[dependencies]]
names = [
    "bat",
    "ripgrep",
    "starship",
]

[[system_dependencies]]
any = ["fish", "moreutils"]

[[user_dependencies]]
requires = ["os=macos"]
wants = { command = "gecho" }
brew = ["coreutils"]
