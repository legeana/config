[[user_dependencies]]
requires = "family=unix"
cargo = "starship"
wants = { command = "starship" }

[[system_dependencies]]
any = "starship"
apt = []
wants = { command = "starship" }

[[system_dependencies]]
# cmake is required for building starship via cargo.
apt = "cmake"
wants = { command = "cmake" }
