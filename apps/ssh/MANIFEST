prefix ~/.ssh
symlink config
symlink_tree config.d

if is_linux {
    if tags 'feature=wsl' {
        subdir wsl
    } else {
        subdir linux
    }
} else if is_macos {
    subdir macos
} else if is_windows {
    subdir windows
}
