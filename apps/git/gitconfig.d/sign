[commit]
gpgsign = true

[tag]
gpgsign = true

[gpg]
format = ssh

[gpg.ssh]
allowedSignersFile = ~/.gitconfig.keys/allowed-signers
revocationFile = ~/.gitconfig.keys/revoked-signers
defaultKeyCommand = git signing-key
